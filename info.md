# Visual Timer Card

A circular visual timer card for Home Assistant that tracks a `timer.*` entity. The arc shrinks as the timer runs down. On completion, it flashes for a configurable duration with a completion message.

See the [README](README.md) for configuration and installation details.
